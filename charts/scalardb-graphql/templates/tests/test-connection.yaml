apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scalardb-graphql.fullname" . }}-test-connection"
  labels:
    {{- include "scalardb-graphql.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scalardb-graphql.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
